# components/modules/CMakeLists.txt — Placeholder for application logic modules
# Will be populated when radar, audio, power, and BLE modules are implemented.

idf_component_register(INCLUDE_DIRS ".")

# Camada de aplicação — módulos funcionais (radar, áudio, energia, BLE)
set(COMPONENT_LAYER "modules")
